Analysis goes in Feishu docs, short replies in chat. No tables. Direct execution, no re-confirming. HTML for blog only.
§
User prefers production-grade databases over embedded ones (warned about H2 usage). For self-hosted apps, always offer PostgreSQL/MySQL setup by default, not H2/SQLite.
§
Chinese. Active SEO (Bing/Baidu verification, sitemaps). Expects direct execution, no re-confirming. Prefers pure-letter domains, PG DBs, 127.0.0.1 Docker binds except SMTP 25. Complex content send as file attachment not markdown. Weekly work summary as Feishu doc on Mondays, placed into knowledge base.
§
For self-hosted web apps behind 1Panel OpenResty: all Docker ports must bind to 127.0.0.1 only, no direct IP catch-all Nginx configs allowed. Access only via domain names through Cloudflare proxy.
§
Prefers pure-letter domain names (no numbers). For brand domains, doesn't want to be limited to a fixed prefix — prefers meaningful keyword-based suggestions.
§
（WF API 已停止运营，不再维护）
§
Dujiao 商城已随 WF API 停止运营。
§
AI 动态简报格式要求：每条4-5条精选，结构为三段式（事件→数据→关注理由），最后加「一句话速览」表格收尾。聚焦 AI Coding 与具身智能方向。语言简洁，直接给结论。